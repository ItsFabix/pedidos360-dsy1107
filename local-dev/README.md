# PostgreSQL local opcional

Solo para probar Spring Data JPA desde el PC.

```powershell
docker compose -f docker-compose.postgres.yml up -d
```

Contraseña `dsy1107-local` es únicamente para el contenedor local de desarrollo.

No reemplaza RDS en la Semana 05.
